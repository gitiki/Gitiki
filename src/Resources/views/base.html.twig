<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block head_title (block('title') ~ ' - ' ~ wiki_name)|raw %}</title>

    <link href="{{ app.request.basepath }}/assets/gitiki.css" rel="stylesheet">

    {% block head '' %}
  </head>
  <body>
    <nav class="navbar navbar-default navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <a class="navbar-brand" href="{{ path('page', { path: '/index.md' }) }}">
            {{ wiki_name }}
          </a>
        </div>

        {{ render(path('_common_menu')) }}
      </div>
    </nav>

    <div id="title">
      <div class="container">
        <h1>{% block title '' %}</h1>
      </div>
    </div>

    {% block content '' %}
  </body>
</html>
